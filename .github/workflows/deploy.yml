name: Deploy sport app

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: sport-app-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to daria
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      DEPLOY_HOST: 84.252.101.38
      DEPLOY_USER: deploy

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install SSH key
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key

      - name: Deploy
        run: |
          ssh -i ~/.ssh/deploy_key \
            -o IdentitiesOnly=yes \
            -o StrictHostKeyChecking=accept-new \
            "${DEPLOY_USER}@${DEPLOY_HOST}" \
            'set -eu
             cd /opt/apps/sport-app
             git pull --ff-only origin main
             docker compose up -d --build
             docker compose ps
             for i in $(seq 1 30); do
               if curl -fsS -I http://127.0.0.1:3202 >/dev/null; then
                 break
               fi
               if [ "$i" -eq 30 ]; then
                 exit 1
               fi
               sleep 2
             done
             sudo caddy validate --config /etc/caddy/Caddyfile
             sudo systemctl reload caddy
             for i in $(seq 1 30); do
               if curl -fsS -I https://sport.as-shamshurin.xyz >/dev/null; then
                 break
               fi
               if [ "$i" -eq 30 ]; then
                 exit 1
               fi
               sleep 2
             done'
